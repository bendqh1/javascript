window.setInterval( () => {
  // Code
}, 5000);

const myInterval = setInterval( () => {
  // Code
}, 2500);
// We put setInterval in a variable in case we would want to clear the internval after some amount of executions;
